# Backend environment example (NO secrets)
#
# Copy to `backend/.env` (ignored by git) and fill values.

PORT=5001
NODE_ENV=development

# Auth
JWT_SECRET=change-me

# Supabase (API REST)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=YOUR_ANON_KEY

# Production-only (recommended if RLS is enabled)
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Optional: direct DB access (only needed for local tooling/migrations that use pg)
DB_HOST=db.YOUR_PROJECT_REF.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=YOUR_DB_PASSWORD

